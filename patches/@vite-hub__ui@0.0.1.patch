diff --git a/dist/index.d.ts b/dist/index.d.ts
index 1e6e1fbe7aff9a7652b20cc501f67edde4270427..d38c99444fff9abd430b98f5b4b9c10f02efbbb4 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -6036,9 +6036,10 @@ interface AgentInvocationConfiguration {
   runtime?: {
     name?: string;
   };
-  tools?: readonly {
-    name: string;
-  }[];
+	tools?: readonly {
+		description?: string;
+		name: string;
+	}[];
   workspace?: {
     mode?: string;
     name?: string;
diff --git a/dist/index.js b/dist/index.js
index 88ec5a5ae251655e49e641233cb5a2c055bd95b9..63faa1bc5f43bcc5f8c5a1b2803378ff431fe296 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -10901,18 +10901,19 @@ const messageRoleLabels = {
 	assistant: "Assistant",
 	system: "System",
 	tool: "Tool",
-	user: "User"
+	user: "You"
 };
 function renderMessage(activity, expanded, toggleExpanded) {
 	const body = activity.body ?? "";
 	const collapsible = activity.role === "user" && (body.length > 720 || body.split(/\r?\n/).length > 12);
 	const isExpanded = expanded.has(activity.id);
+	const role = activity.role ?? "assistant";
 	return h("li", {
 		class: "vh-invocation-message",
-		"data-role": activity.role,
+		"data-role": role,
 		key: activity.id
 	}, [
-		h("span", { class: "vh-visually-hidden" }, `${messageRoleLabels[activity.role ?? "assistant"]} message`),
+		h("span", { class: "vh-invocation-message__label" }, messageRoleLabels[role]),
 		h("div", {
 			class: "vh-invocation-message__content",
 			"data-collapsed": collapsible && !isExpanded ? "true" : void 0
@@ -11081,7 +11082,13 @@ function renderEvent(activity, inspect) {
 			onClick: () => inspect(inspectTarget),
 			type: "button"
 		}, `Inspect ${inspectTarget}`) : null
-	]) : null]), deliveryBody ? h("div", { class: "vh-invocation-delivery__body" }, [markdown(deliveryBody, "vh-invocation-event__markdown")]) : null]);
+	]) : null]), deliveryBody ? h("div", {
+		class: "vh-invocation-delivery__body vh-invocation-message",
+		"data-role": "assistant"
+	}, [
+		h("span", { class: "vh-invocation-message__label" }, "Assistant"),
+		h("div", { class: "vh-invocation-message__content" }, [markdown(deliveryBody, "vh-invocation-message__body")])
+	]) : null]);
 }
 function renderEventPayload(label, value) {
 	if (value === void 0) return null;
@@ -11374,16 +11381,153 @@ function inspectorDisclosure(title, summary, body) {
 		renderChevronDown("vh-invocation-inspector__chevron")
 	]), body]);
 }
-function renderConfiguration(configuration) {
-	const driver = driverLabel(configuration);
+const invocationBrands = {
+	openrouter: { label: "OpenRouter", logo: "openrouter" },
+	"z-ai": { label: "Z.AI", logo: "z-ai" }
+};
+const invocationToolDescriptions = {
+	blob_edit: "Put or delete Blob objects from an attachment, workspace file, or provided body.",
+	blob_read: "Read Blob contents or metadata, or list objects under a prefix.",
+	db_exec: "Run a database mutation with a recorded rationale.",
+	db_query: "Run one read-only query against the configured database.",
+	db_schema: "Describe the configured database schema."
+};
+function invocationToolDescription(tool) {
+	return tool.description ?? invocationToolDescriptions[tool.name];
+}
+function invocationBrand(value) {
+	const id = String(value ?? "").trim().toLocaleLowerCase();
+	if (!id) return;
+	return invocationBrands[id] ?? {
+		label: id.split(/[._-]/g).filter(Boolean).map((part) => part.length <= 3 ? part.toLocaleUpperCase() : `${part[0].toLocaleUpperCase()}${part.slice(1)}`).join(" "),
+		logo: "fallback"
+	};
+}
+function renderInvocationBrandLogo(brand, className = "") {
+	if (!brand) return null;
+	if (brand.logo === "openrouter") return h("svg", {
+		"aria-hidden": "true",
+		class: `vh-invocation-brand__logo ${className}`.trim(),
+		"data-brand": "openrouter",
+		viewBox: "0 0 401.4 293.7"
+	}, [h("path", { d: "M303.9475,17.19926c42.79734,0,77.48933,34.69327,77.48933,77.48933s-34.69199,77.48933-77.48933,77.48933l76.86166,76.86244c9.76367,9.76313,2.84903,26.45667-10.95697,26.45667h-220.88335c-71.32686,0-129.14889-57.82202-129.14889-129.14889S77.64197,17.19926,148.96884,17.19926h154.97866ZM148.96884,68.85881c-42.79607,0-77.48933,34.69327-77.48933,77.48933s34.69327,77.48933,77.48933,77.48933,77.48933-34.69327,77.48933-77.48933-34.69327-77.48933-77.48933-77.48933Z" })]);
+	if (brand.logo === "z-ai") return h("svg", {
+		"aria-hidden": "true",
+		class: `vh-invocation-brand__logo ${className}`.trim(),
+		"data-brand": "z-ai",
+		viewBox: "0 0 30 30"
+	}, [
+		h("rect", {
+			fill: "#2d2d2d",
+			height: "28",
+			rx: "4",
+			width: "28",
+			x: "1",
+			y: "1"
+		}),
+		h("path", {
+			d: "M15.47 7.1l-1.3 1.85c-.2.29-.54.47-.9.47h-7.1V7.09h9.3Zm8.83 0L13.14 22.91H5.7L16.86 7.1h7.44Zm-9.77 15.81 1.31-1.86c.2-.29.54-.47.9-.47h7.09v2.33h-9.3Z",
+			fill: "#fff"
+		})
+	]);
+	return h("span", {
+		"aria-hidden": "true",
+		class: `vh-invocation-brand__fallback ${className}`.trim()
+	}, brand.label.slice(0, 1));
+}
+function modelDisplayName(id) {
+	const slug = String(id ?? "").split("/").at(-1) ?? "";
+	return slug.split("-").filter(Boolean).map((part) => {
+		const lower = part.toLocaleLowerCase();
+		if (/^(ai|gpt|glm|llm|vlm)$/.test(lower)) return lower.toLocaleUpperCase();
+		if (/^\d+[a-z]$/i.test(part)) return part.toLocaleUpperCase();
+		return `${part[0]?.toLocaleUpperCase() ?? ""}${part.slice(1)}`;
+	}).join(" ") || "Configured model";
+}
+function renderExecutionIdentity(configuration) {
+	const driver = configuration.driver;
+	const model = driver?.model;
+	if (!model?.id && !model?.provider && !driver?.provider) return null;
+	const makerId = model?.id?.includes("/") ? model.id.split("/")[0] : void 0;
+	const maker = invocationBrand(makerId);
+	const provider = invocationBrand(model?.provider ?? driver?.provider);
+	const runtime = configuration.runtime?.name;
+	return h("div", { class: "vh-invocation-execution" }, [
+		model?.id ? h("div", {
+			class: "vh-invocation-execution__model",
+			title: model.id
+		}, [
+			renderInvocationBrandLogo(maker, "vh-invocation-execution__model-logo"),
+			h("div", [
+				h("strong", modelDisplayName(model.id)),
+				maker ? h("span", `by ${maker.label}`) : h("code", model.id)
+			])
+		]) : null,
+		provider ? h("div", { class: "vh-invocation-execution__route" }, [
+			h("span", "via"),
+			renderInvocationBrandLogo(provider, "vh-invocation-execution__provider-logo"),
+			h("strong", provider.label)
+		]) : null,
+		runtime && runtime.toLocaleLowerCase() !== "unknown" ? h("span", { class: "vh-invocation-execution__runtime" }, runtime) : null
+	]);
+}
+function invocationToolUsage(invocation) {
+	const counts = /* @__PURE__ */ new Map();
+	const seen = /* @__PURE__ */ new Set();
+	for (const observation of invocation.observations ?? []) {
+		const attributes = observation.attributes;
+		if (!attributes || typeof attributes !== "object") continue;
+		const name = attributes["tool.name"];
+		if (typeof name !== "string" || !name) continue;
+		const id = attributes["tool.id"];
+		if (typeof id === "string" && id) {
+			const key = `${name}:${id}`;
+			if (seen.has(key)) continue;
+			seen.add(key);
+		} else if (!String(observation.name ?? "").endsWith(".start")) continue;
+		counts.set(name, (counts.get(name) ?? 0) + 1);
+	}
+	return counts;
+}
+function renderConfiguredTools(tools, invocation) {
+	const counts = invocationToolUsage(invocation);
+	const rows = [...tools].sort((left, right) => {
+		const useDifference = (counts.get(right.name) ?? 0) - (counts.get(left.name) ?? 0);
+		return useDifference || left.name.localeCompare(right.name);
+	});
+	const used = rows.filter((tool) => (counts.get(tool.name) ?? 0) > 0).length;
+	return h("div", { class: "vh-invocation-inspector__group" }, [
+		h("div", { class: "vh-invocation-inspector__group-heading" }, [
+			h("strong", "Tools"),
+			h("small", `${used} of ${rows.length} used`)
+		]),
+		h("ol", { class: "vh-invocation-tool-list" }, rows.map((tool) => {
+			const count = counts.get(tool.name) ?? 0;
+			const description = invocationToolDescription(tool);
+			return h("li", {
+				"data-used": count ? "true" : "false",
+				key: tool.name
+			}, [
+				h("div", { class: "vh-invocation-tool-list__heading" }, [
+					h("code", tool.name),
+					h("span", {
+						"aria-label": count ? `${count} call${count === 1 ? "" : "s"}` : "Not used",
+						class: "vh-invocation-tool-list__count"
+					}, count || "—")
+				]),
+				description ? h("p", { title: description }, description) : null
+			]);
+		}))
+	]);
+}
+function renderConfiguration(configuration, invocation) {
+	const execution = renderExecutionIdentity(configuration);
 	const workspace = workspaceLabel(configuration);
 	const setup = [
-		inspectorRow("Driver", driver),
-		inspectorRow("Runtime", configuration.runtime?.name),
 		inspectorRow("Workspace", workspace)
 	].filter((item) => item !== null);
 	const groups = [
-		setup.length ? h("div", { class: "vh-invocation-inspector__group" }, [h("div", { class: "vh-invocation-inspector__group-heading" }, [h("strong", "Execution")]), h("dl", { class: "vh-invocation-inspector__list" }, setup)]) : null,
+		execution ? h("div", { class: "vh-invocation-inspector__group vh-invocation-inspector__group--execution" }, [h("div", { class: "vh-invocation-inspector__group-heading" }, [h("strong", "Execution")]), execution, setup.length ? h("dl", { class: "vh-invocation-inspector__list" }, setup) : null]) : setup.length ? h("div", { class: "vh-invocation-inspector__group" }, [h("div", { class: "vh-invocation-inspector__group-heading" }, [h("strong", "Execution")]), h("dl", { class: "vh-invocation-inspector__list" }, setup)]) : null,
 		configuration.workspace?.sources?.length ? inspectorCollection("Sources", configuration.workspace.sources) : null,
 		configuration.channels?.length ? inspectorCollection("Channels", configuration.channels.map((channel) => channel.id === channel.kind ? channel.id : `${channel.id} · ${channel.kind}`)) : null,
 		configuration.capabilities?.length ? h("div", { class: "vh-invocation-inspector__group" }, [h("div", { class: "vh-invocation-inspector__group-heading" }, [h("strong", "Capabilities"), h("small", configuration.capabilities.length)]), h("div", { class: "vh-invocation-inspector__stack" }, configuration.capabilities.map((capability) => capability.metadata ? h("details", {
@@ -11397,7 +11541,7 @@ function renderConfiguration(configuration) {
 			class: "vh-invocation-inspector__item",
 			key: capability.id
 		}, [h("code", capability.id)])))]) : null,
-		configuration.tools?.length ? inspectorCollection("Tools", configuration.tools.map((tool) => tool.name)) : null,
+		configuration.tools?.length ? renderConfiguredTools(configuration.tools, invocation) : null,
 		configuration.instructions?.length ? inspectorDisclosure("Instructions", `${configuration.instructions.length} block${configuration.instructions.length === 1 ? "" : "s"}`, h("pre", { class: "vh-invocation-inspector__instructions" }, configuration.instructions.join("\n\n"))) : null
 	].filter((item) => item !== null);
 	return [configuration.truncated ? h("div", {
@@ -11720,7 +11864,7 @@ const AgentInvocationInspector = defineComponent({
 					metrics.value.tokens !== void 0 ? h("div", [h("dt", "Tokens"), h("dd", new Intl.NumberFormat("en").format(metrics.value.tokens))]) : null
 				])),
 				props.showTimeline ? traceTimeline(activities.value, props.invocation, (id) => emit("selectActivity", id)) : null,
-				...configuration ? renderConfiguration(configuration) : [],
+				...configuration ? renderConfiguration(configuration, props.invocation) : [],
 				slots.metadata?.({ invocation: props.invocation }),
 				inspectorSection("Identifiers", h("div", { class: "vh-invocation-inspector__copy-list" }, [copyAction("trace", "Trace ID", props.invocation.traceId), copyAction("invocation", "Invocation ID", props.invocation.id)]))
 			])]);
diff --git a/dist/styles.css b/dist/styles.css
index de0af2aa954d8ed95f03d1908366083c1f98a733..6dbca229f2dc41e4a4d305ad9a28dcc1ae1ac14a 100644
--- a/dist/styles.css
+++ b/dist/styles.css
@@ -687,19 +687,35 @@
 }
 
 .vh-invocation-message {
+  --vh-invocation-message-bg: color-mix(in oklab, var(--vh-ui-bg-elevated) 34%, transparent);
+  background: var(--vh-invocation-message-bg);
+  border: 1px solid var(--vh-ui-border);
+  border-radius: 1rem;
+  display: flex;
+  flex-direction: column;
+  gap: 0.35rem;
+  margin-block: 0.5rem 1rem;
+  max-width: 82%;
   min-width: 0;
+  padding: 0.75rem 0.875rem;
 }
 
 .vh-invocation-message[data-role="assistant"] {
-  padding: 0.125rem 0.25rem 0.5rem;
+  border-start-start-radius: 0.375rem;
+  margin-inline-end: auto;
 }
 
 .vh-invocation-message[data-role="user"] {
-  background: var(--vh-ui-bg-elevated);
-  border-radius: 1rem;
-  margin-block: 0.5rem 1rem;
-  margin-inline-start: 20%;
-  padding: 0.75rem;
+  --vh-invocation-message-bg: var(--vh-ui-bg-elevated);
+  border-end-end-radius: 0.375rem;
+  margin-inline-start: auto;
+}
+
+.vh-invocation-message__label {
+  color: var(--vh-ui-dimmed);
+  font-size: 0.6875rem;
+  font-weight: 600;
+  line-height: 1;
 }
 
 .vh-invocation-message__body {
@@ -718,7 +734,7 @@
 }
 
 .vh-invocation-message__content[data-collapsed="true"]::after {
-  background: linear-gradient(to bottom, transparent, var(--vh-ui-bg-elevated));
+  background: linear-gradient(to bottom, transparent, var(--vh-invocation-message-bg));
   bottom: 0;
   content: "";
   height: 3.5rem;
@@ -1451,15 +1467,12 @@ details.vh-invocation-event > .vh-invocation-event__summary:hover {
 }
 
 .vh-invocation-delivery__body {
-  color: var(--vh-ui-text);
-  margin: 0.125rem 0 0 2rem;
-  max-width: 42rem;
-  padding-block-end: 0.75rem;
+  margin: 0.35rem 0 1rem 2rem;
+  max-width: min(42rem, calc(82% - 2rem));
 }
 
-.vh-invocation-delivery__body .vh-invocation-event__markdown {
-  font-size: 0.875rem;
-  line-height: 1.625;
+.vh-invocation-delivery__body .vh-invocation-message__body {
+  margin: 0;
 }
 
 .vh-invocation-event__body,
@@ -1680,19 +1693,20 @@ details.vh-invocation-event > .vh-invocation-event__summary:hover {
 
 .vh-invocation-inspector__error,
 .vh-invocation-inspector__notice {
-  border: 1px solid color-mix(in oklab, currentColor 18%, transparent);
-  border-radius: calc(var(--vh-ui-radius) * 1.1);
   margin-block-start: 0.9rem;
-  padding: 0.7rem 0.75rem;
+  padding: 0.6rem 0.7rem;
 }
 
 .vh-invocation-inspector__error {
   background: color-mix(in oklab, var(--vh-ui-error) 7%, transparent);
+  border: 1px solid color-mix(in oklab, currentColor 18%, transparent);
+  border-radius: calc(var(--vh-ui-radius) * 1.1);
   color: var(--vh-ui-error);
 }
 
 .vh-invocation-inspector__notice {
   background: color-mix(in oklab, var(--ui-warning, #a16207) 7%, transparent);
+  border-inline-start: 2px solid color-mix(in oklab, currentColor 55%, transparent);
   color: var(--ui-warning, #a16207);
   margin-block-start: 1rem;
 }
@@ -1733,18 +1747,20 @@ details.vh-invocation-event > .vh-invocation-event__summary:hover {
 }
 
 .vh-invocation-inspector__metrics {
+  border-block: 1px solid var(--vh-ui-border);
   display: grid;
-  gap: 0.375rem;
+  gap: 0;
   grid-template-columns: repeat(auto-fit, minmax(4.75rem, 1fr));
   margin: 0;
 }
 
 .vh-invocation-inspector__metrics > div {
-  background: color-mix(in oklab, var(--vh-ui-bg-elevated) 72%, transparent);
-  border: 1px solid var(--vh-ui-border);
-  border-radius: calc(var(--vh-ui-radius) * 1.05);
   min-width: 0;
-  padding: 0.55rem 0.6rem;
+  padding: 0.6rem 0.65rem;
+}
+
+.vh-invocation-inspector__metrics > div + div {
+  border-inline-start: 1px solid var(--vh-ui-border);
 }
 
 .vh-invocation-inspector__metrics dt {
@@ -1888,18 +1904,12 @@ details.vh-invocation-event > .vh-invocation-event__summary:hover {
 }
 
 .vh-invocation-inspector__groups {
-  border: 1px solid var(--vh-ui-border);
-  border-radius: calc(var(--vh-ui-radius) * 1.2);
-  overflow: hidden;
+  display: grid;
+  gap: 1.1rem;
 }
 
 .vh-invocation-inspector__group {
   min-width: 0;
-  padding: 0.7rem 0.75rem;
-}
-
-.vh-invocation-inspector__group + .vh-invocation-inspector__group {
-  border-block-start: 1px solid var(--vh-ui-border);
 }
 
 .vh-invocation-inspector__group-heading {
@@ -1919,6 +1929,114 @@ details.vh-invocation-event > .vh-invocation-event__summary:hover {
   min-width: 0;
 }
 
+.vh-invocation-inspector__group--execution {
+  background:
+    radial-gradient(circle at 95% 0%, color-mix(in oklab, var(--ui-primary, #2563eb) 9%, transparent), transparent 48%),
+    color-mix(in oklab, var(--vh-ui-bg-elevated) 54%, transparent);
+  border: 1px solid color-mix(in oklab, var(--vh-ui-border) 78%, transparent);
+  border-radius: calc(var(--vh-ui-radius) * 1.2);
+  padding: 0.8rem;
+}
+
+.vh-invocation-execution {
+  display: grid;
+  gap: 0.65rem;
+  margin-block-start: 0.65rem;
+}
+
+.vh-invocation-execution__model {
+  align-items: center;
+  display: grid;
+  gap: 0.7rem;
+  grid-template-columns: 2.35rem minmax(0, 1fr);
+  min-width: 0;
+}
+
+.vh-invocation-execution__model > div {
+  display: grid;
+  gap: 0.08rem;
+  min-width: 0;
+}
+
+.vh-invocation-execution__model strong {
+  color: var(--vh-ui-text);
+  font-size: 0.875rem;
+  font-weight: 670;
+  letter-spacing: -0.015em;
+  line-height: 1.3;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+.vh-invocation-execution__model span,
+.vh-invocation-execution__model code {
+  color: var(--vh-ui-dimmed);
+  font-size: 0.6875rem;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+.vh-invocation-brand__logo,
+.vh-invocation-brand__fallback {
+  display: block;
+  flex: none;
+}
+
+.vh-invocation-brand__logo {
+  height: 1rem;
+  width: 1rem;
+}
+
+.vh-invocation-brand__logo[data-brand="openrouter"] {
+  color: #7624f4;
+  fill: currentColor;
+}
+
+.vh-invocation-execution__model-logo,
+.vh-invocation-brand__fallback.vh-invocation-execution__model-logo {
+  height: 2.35rem;
+  width: 2.35rem;
+}
+
+.vh-invocation-brand__fallback {
+  align-items: center;
+  background: color-mix(in oklab, var(--ui-primary, #2563eb) 10%, var(--vh-ui-bg));
+  border: 1px solid color-mix(in oklab, var(--ui-primary, #2563eb) 20%, var(--vh-ui-border));
+  border-radius: calc(var(--vh-ui-radius) * 0.9);
+  color: var(--vh-ui-muted);
+  font-size: 0.65rem;
+  font-weight: 700;
+  justify-content: center;
+}
+
+.vh-invocation-brand__fallback.vh-invocation-execution__provider-logo {
+  display: inline-flex;
+  height: 1rem;
+  width: 1rem;
+}
+
+.vh-invocation-execution__route {
+  align-items: center;
+  color: var(--vh-ui-dimmed);
+  display: flex;
+  font-size: 0.6875rem;
+  gap: 0.35rem;
+  margin-inline-start: 3.05rem;
+}
+
+.vh-invocation-execution__route strong {
+  color: var(--vh-ui-muted);
+  font-weight: 600;
+}
+
+.vh-invocation-execution__runtime {
+  color: var(--vh-ui-dimmed);
+  font: 0.625rem/1.4 ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
+  margin-inline-start: 3.05rem;
+}
+
 .vh-invocation-inspector__group-heading small {
   color: var(--vh-ui-dimmed);
   font-size: 0.625rem;
@@ -1984,9 +2102,7 @@ summary.vh-invocation-inspector__group-heading:focus-visible,
 }
 
 .vh-invocation-inspector__copy-list {
-  border: 1px solid var(--vh-ui-border);
-  border-radius: calc(var(--vh-ui-radius) * 1.05);
-  overflow: hidden;
+  border-block: 1px solid var(--vh-ui-border);
 }
 
 .vh-invocation-inspector__copy {
@@ -2000,8 +2116,8 @@ summary.vh-invocation-inspector__group-heading:focus-visible,
   font: inherit;
   gap: 0.5rem;
   grid-template-columns: minmax(0, 1fr) auto 1rem;
-  min-height: 2.4rem;
-  padding: 0.45rem 0.65rem;
+  min-height: 2.25rem;
+  padding: 0.4rem 0.25rem;
   text-align: start;
   transition:
     background-color 120ms ease,
@@ -2041,6 +2157,17 @@ summary.vh-invocation-inspector__group-heading:focus-visible,
   justify-content: center;
 }
 
+@media (hover: hover) and (pointer: fine) {
+  .vh-invocation-inspector__copy-icon {
+    opacity: 0;
+    transition: opacity 140ms ease;
+  }
+
+  .vh-invocation-inspector__copy:is(:hover, :focus-visible) .vh-invocation-inspector__copy-icon {
+    opacity: 1;
+  }
+}
+
 .vh-invocation-inspector__copy-icon svg {
   height: 0.875rem;
   stroke: currentColor;
@@ -2105,6 +2232,66 @@ summary.vh-invocation-inspector__group-heading:focus-visible,
   overflow-wrap: anywhere;
 }
 
+.vh-invocation-tool-list {
+  border-block: 1px solid var(--vh-ui-border);
+  list-style: none;
+  margin: 0.55rem 0 0;
+  padding: 0;
+}
+
+.vh-invocation-tool-list > li {
+  min-width: 0;
+  padding: 0.55rem 0.15rem 0.6rem;
+}
+
+.vh-invocation-tool-list > li + li {
+  border-block-start: 1px solid color-mix(in oklab, var(--vh-ui-border) 72%, transparent);
+}
+
+.vh-invocation-tool-list > li[data-used="false"] {
+  opacity: 0.66;
+}
+
+.vh-invocation-tool-list__heading {
+  align-items: baseline;
+  display: grid;
+  gap: 0.75rem;
+  grid-template-columns: minmax(0, 1fr) auto;
+}
+
+.vh-invocation-tool-list__heading code {
+  color: var(--vh-ui-text);
+  font-size: 0.6875rem;
+  font-weight: 580;
+  overflow-wrap: anywhere;
+}
+
+.vh-invocation-tool-list__count {
+  color: var(--vh-ui-dimmed);
+  font: 0.6875rem/1.3 ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
+  font-variant-numeric: tabular-nums;
+  min-width: 1.5rem;
+  text-align: end;
+}
+
+.vh-invocation-tool-list > li[data-used="true"] .vh-invocation-tool-list__count {
+  background: color-mix(in oklab, var(--vh-ui-success) 11%, transparent);
+  border-radius: 999px;
+  color: color-mix(in oklab, var(--vh-ui-success) 60%, var(--vh-ui-text));
+  padding: 0.08rem 0.4rem;
+}
+
+.vh-invocation-tool-list p {
+  color: var(--vh-ui-dimmed);
+  display: -webkit-box;
+  font-size: 0.65625rem;
+  -webkit-box-orient: vertical;
+  -webkit-line-clamp: 2;
+  line-height: 1.45;
+  margin: 0.22rem 2.25rem 0 0;
+  overflow: hidden;
+}
+
 .vh-invocation-inspector__item-disclosure pre,
 .vh-invocation-inspector__instructions {
   background: color-mix(in oklab, var(--vh-ui-bg-elevated) 65%, transparent);
@@ -2158,8 +2345,12 @@ summary.vh-invocation-inspector__group-heading:focus-visible,
     padding: 1.5rem 1rem 3rem;
   }
 
-  .vh-invocation-message[data-role="user"] {
-    margin-inline-start: 1rem;
+  .vh-invocation-message {
+    max-width: calc(100% - 1rem);
+  }
+
+  .vh-invocation-delivery__body {
+    max-width: calc(100% - 3rem);
   }
 }
 
@@ -2168,8 +2359,12 @@ summary.vh-invocation-inspector__group-heading:focus-visible,
     padding: 1.5rem 1rem 3rem;
   }
 
-  .vh-invocation-message[data-role="user"] {
-    margin-inline-start: 1rem;
+  .vh-invocation-message {
+    max-width: calc(100% - 1rem);
+  }
+
+  .vh-invocation-delivery__body {
+    max-width: calc(100% - 3rem);
   }
 }
 
