diff --git a/dist/agents-CohvvR2a.js b/dist/agents-CohvvR2a.js
index 20063ce08c9a093d0c3893ba985eb0bc040beedf..f94690be56b16e77dd5eac74410c5c38c8904744 100644
--- a/dist/agents-CohvvR2a.js
+++ b/dist/agents-CohvvR2a.js
@@ -1,6 +1,7 @@
-import { n as consoleInvocationsFallbackKey, p as resolveConsoleInvocations } from "./internal-DFtRVKA0.js";
+import { n as consoleInvocationsFallbackKey, o as installConsoleInvocationFallback, p as resolveConsoleInvocations } from "./internal-DFtRVKA0.js";
 //#region src/console/runtime/server/agents.ts
 const consoleAgentsKey = Symbol.for("vitehub.console.agents");
+const consoleInvocationsRootKey = Symbol.for("vitehub.console.invocations.root");
 const consoleAssignedInvocations = /* @__PURE__ */ new WeakMap();
 function installConsoleAgents(agentNames, invocations) {
 	const agents = [...new Set(agentNames.map((name) => name.trim()).filter(Boolean))].sort();
@@ -9,7 +10,8 @@ function installConsoleAgents(agentNames, invocations) {
 	return agents;
 }
 function installConsoleAgentDefinitions(entries, invocations) {
-	return installConsoleAgents(entries.map(({ definition, fallbackName }) => {
+	let configuredInvocations;
+	const agentNames = entries.map(({ definition, fallbackName }) => {
 		const module = definition && typeof definition === "object" ? definition : void 0;
 		let agent = module;
 		if (module?.default && typeof module.default === "object") agent = module.default;
@@ -18,10 +20,17 @@ function installConsoleAgentDefinitions(entries, invocations) {
 			if (agent.invocations === void 0 || agent.invocations === assigned) {
 				agent.invocations = invocations;
 				consoleAssignedInvocations.set(agent, invocations);
-			} else if (assigned) consoleAssignedInvocations.delete(agent);
+			} else {
+				configuredInvocations ??= agent.invocations;
+				if (assigned) consoleAssignedInvocations.delete(agent);
+			}
 		}
 		return typeof agent?.name === "string" && agent.name.trim() ? agent.name : fallbackName;
-	}), invocations);
+	});
+	const installedInvocations = configuredInvocations ?? invocations;
+	const projectRoot = globalThis[consoleInvocationsRootKey];
+	if (configuredInvocations && typeof projectRoot === "string") installConsoleInvocationFallback(installedInvocations, projectRoot, globalThis, `agent:${projectRoot}`);
+	return installConsoleAgents(agentNames, installedInvocations);
 }
 function getConsoleAgents() {
 	return resolveConsoleInvocations()?.[consoleAgentsKey] ?? [];
diff --git a/dist/console/runtime/public/console/console-invocation-overrides.js b/dist/console/runtime/public/console/console-invocation-overrides.js
new file mode 100644
index 0000000000000000000000000000000000000000..a2fba6114e589f96ee679f3fad70016f6e3fa24d
--- /dev/null
+++ b/dist/console/runtime/public/console/console-invocation-overrides.js
@@ -0,0 +1,375 @@
+(() => {
+  const descriptions = {
+    blob_edit: "Put or delete Blob objects from an attachment, workspace file, or provided body.",
+    blob_read: "Read Blob contents or metadata, or list objects under a prefix.",
+    db_exec: "Run a database mutation with a recorded rationale.",
+    db_query: "Run one read-only query against the configured database.",
+    db_schema: "Describe the configured database schema.",
+  };
+  let detail;
+  let scheduled = false;
+
+  const nativeFetch = window.fetch.bind(window);
+  window.fetch = async (...args) => {
+    const response = await nativeFetch(...args);
+    const url = String(args[0] instanceof Request ? args[0].url : args[0]);
+    if (/\/api\/_vitehub\/console\/invocations\/[^/?]+/.test(url)) {
+      void response.clone().json().then((value) => {
+        detail = value;
+        schedule();
+      }).catch(() => {});
+    }
+    return response;
+  };
+
+  function element(tag, className, text) {
+    const node = document.createElement(tag);
+    if (className) node.className = className;
+    if (text !== undefined) node.textContent = text;
+    return node;
+  }
+
+  function telegramMark(className = "") {
+    const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
+    svg.setAttribute("aria-hidden", "true");
+    svg.setAttribute("class", `vh-channel__logo ${className}`.trim());
+    svg.setAttribute("viewBox", "0 0 24 24");
+    const path = document.createElementNS(svg.namespaceURI, "path");
+    path.setAttribute("d", "M21.6 3.1 18.4 20c-.24 1.2-.87 1.5-1.77.94l-4.88-3.6-2.35 2.27c-.26.26-.48.48-.98.48l.35-4.97 9.04-8.17c.39-.35-.09-.55-.61-.2L6.03 13.8l-4.81-1.5c-1.05-.33-1.07-1.05.22-1.55L20.25 3.5c.87-.32 1.63.2 1.35-.4Z");
+    svg.append(path);
+    return svg;
+  }
+
+  function channelMeta(label) {
+    const meta = element("span", "vh-channel");
+    meta.append(element("strong", "", label), element("span", "vh-channel__separator", "via"), telegramMark(), element("span", "", "Telegram"));
+    return meta;
+  }
+
+  function copyIcon() {
+    const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
+    svg.setAttribute("aria-hidden", "true");
+    svg.setAttribute("viewBox", "0 0 24 24");
+    const rect = document.createElementNS(svg.namespaceURI, "rect");
+    rect.setAttribute("x", "8");
+    rect.setAttribute("y", "8");
+    rect.setAttribute("width", "12");
+    rect.setAttribute("height", "12");
+    rect.setAttribute("rx", "2");
+    const path = document.createElementNS(svg.namespaceURI, "path");
+    path.setAttribute("d", "M16 8V6a2 2 0 0 0-2-2H6a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2h2");
+    svg.append(rect, path);
+    return svg;
+  }
+
+  function showToast(message) {
+    let toast = document.querySelector(".vh-console-toast");
+    if (!toast) {
+      toast = element("div", "vh-console-toast");
+      toast.setAttribute("role", "status");
+      toast.setAttribute("aria-live", "polite");
+      document.body.append(toast);
+    }
+    toast.textContent = message;
+    toast.dataset.visible = "true";
+    window.clearTimeout(showToast.timer);
+    showToast.timer = window.setTimeout(() => {
+      toast.dataset.visible = "false";
+    }, 1800);
+  }
+
+  async function copyCurrentUrl() {
+    try {
+      await navigator.clipboard.writeText(window.location.href);
+    } catch {
+      const field = element("textarea");
+      field.value = window.location.href;
+      field.style.position = "fixed";
+      field.style.opacity = "0";
+      document.body.append(field);
+      field.select();
+      document.execCommand("copy");
+      field.remove();
+    }
+    showToast("Link copied");
+  }
+
+  function configuration() {
+    let result = detail?.invocation?.configuration ?? {};
+    for (const observation of detail?.observations ?? []) {
+      const next = observation?.attributes?.["vitehub.agent.configuration"];
+      if (!next || typeof next !== "object") continue;
+      result = {
+        ...result,
+        ...next,
+        driver: {
+          ...result.driver,
+          ...next.driver,
+          model: { ...result.driver?.model, ...next.driver?.model },
+        },
+      };
+    }
+    return result;
+  }
+
+  function usageCounts() {
+    const counts = new Map();
+    const seen = new Set();
+    for (const observation of detail?.observations ?? []) {
+      const name = observation?.attributes?.["tool.name"];
+      if (typeof name !== "string" || !name) continue;
+      const id = observation.attributes["tool.id"];
+      if (typeof id === "string" && id) {
+        const key = `${name}:${id}`;
+        if (seen.has(key)) continue;
+        seen.add(key);
+      } else if (!String(observation.name ?? "").endsWith(".start")) continue;
+      counts.set(name, (counts.get(name) ?? 0) + 1);
+    }
+    return counts;
+  }
+
+  function brand(value) {
+    const id = String(value ?? "").trim().toLowerCase();
+    if (!id) return;
+    if (id === "openrouter") return { id, label: "OpenRouter" };
+    if (id === "z-ai") return { id, label: "Z.AI" };
+    return {
+      id: "fallback",
+      label: id.split(/[._-]/g).filter(Boolean).map((part) => part.length <= 3 ? part.toUpperCase() : `${part[0].toUpperCase()}${part.slice(1)}`).join(" "),
+    };
+  }
+
+  function brandMark(value, className) {
+    if (!value) return;
+    if (value.id === "z-ai") {
+      const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
+      svg.setAttribute("aria-hidden", "true");
+      svg.setAttribute("class", `vh-invocation-brand__logo ${className}`);
+      svg.setAttribute("viewBox", "0 0 30 30");
+      const box = document.createElementNS(svg.namespaceURI, "rect");
+      box.setAttribute("fill", "#2d2d2d");
+      box.setAttribute("height", "28");
+      box.setAttribute("rx", "4");
+      box.setAttribute("width", "28");
+      box.setAttribute("x", "1");
+      box.setAttribute("y", "1");
+      const path = document.createElementNS(svg.namespaceURI, "path");
+      path.setAttribute("d", "M15.47 7.1l-1.3 1.85c-.2.29-.54.47-.9.47h-7.1V7.09h9.3Zm8.83 0L13.14 22.91H5.7L16.86 7.1h7.44Zm-9.77 15.81 1.31-1.86c.2-.29.54-.47.9-.47h7.09v2.33h-9.3Z");
+      path.setAttribute("fill", "#fff");
+      svg.append(box, path);
+      return svg;
+    }
+    if (value.id === "openrouter") {
+      const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
+      svg.setAttribute("aria-hidden", "true");
+      svg.setAttribute("class", `vh-invocation-brand__logo ${className}`);
+      svg.setAttribute("viewBox", "0 0 401.4 293.7");
+      const path = document.createElementNS(svg.namespaceURI, "path");
+      path.setAttribute("d", "M303.9475,17.19926c42.79734,0,77.48933,34.69327,77.48933,77.48933s-34.69199,77.48933-77.48933,77.48933l76.86166,76.86244c9.76367,9.76313,2.84903,26.45667-10.95697,26.45667h-220.88335c-71.32686,0-129.14889-57.82202-129.14889-129.14889S77.64197,17.19926,148.96884,17.19926h154.97866ZM148.96884,68.85881c-42.79607,0-77.48933,34.69327-77.48933,77.48933s34.69327,77.48933,77.48933,77.48933,77.48933-34.69327,77.48933-77.48933-34.69327-77.48933-77.48933-77.48933Z");
+      svg.append(path);
+      return svg;
+    }
+    return element("span", `vh-invocation-brand__fallback ${className}`, value.label.slice(0, 1));
+  }
+
+  function modelName(value) {
+    const slug = String(value ?? "").split("/").at(-1) ?? "";
+    return slug.split("-").filter(Boolean).map((part) => {
+      const lower = part.toLowerCase();
+      if (/^(ai|gpt|glm|llm|vlm)$/.test(lower)) return lower.toUpperCase();
+      if (/^\d+[a-z]$/i.test(part)) return part.toUpperCase();
+      return `${part[0]?.toUpperCase() ?? ""}${part.slice(1)}`;
+    }).join(" ") || "Configured model";
+  }
+
+  function enhanceExecution(group, config) {
+    const model = config.driver?.model;
+    if (!model?.id) return;
+    const version = `${model.id}:${model.provider ?? config.driver?.provider ?? ""}`;
+    if (group.dataset.vhExecution === version) return;
+    group.dataset.vhExecution = version;
+    group.classList.add("vh-invocation-inspector__group--execution");
+    group.querySelector(":scope > .vh-invocation-inspector__group-heading")?.remove();
+    group.querySelector(":scope > dl")?.remove();
+    group.querySelector(":scope > .vh-invocation-execution")?.remove();
+    const maker = brand(model.id.includes("/") ? model.id.split("/")[0] : undefined);
+    const provider = brand(model.provider ?? config.driver?.provider);
+    const card = element("div", "vh-invocation-execution");
+    const identity = element("div", "vh-invocation-execution__model");
+    identity.title = model.id;
+    identity.append(brandMark(maker, "vh-invocation-execution__model-logo"));
+    const names = element("div");
+    names.append(element("strong", "", modelName(model.id)));
+    if (maker) names.append(element("span", "", `by ${maker.label}`));
+    identity.append(names);
+    card.append(identity);
+    if (provider) {
+      const route = element("div", "vh-invocation-execution__route");
+      route.append(element("span", "", "via"), brandMark(provider, "vh-invocation-execution__provider-logo"), element("strong", "", provider.label));
+      card.append(route);
+    }
+    group.append(card);
+  }
+
+  function enhanceTools(group, tools) {
+    const counts = usageCounts();
+    const rows = [...tools].sort((left, right) => (counts.get(right.name) ?? 0) - (counts.get(left.name) ?? 0) || left.name.localeCompare(right.name));
+    const signature = rows.map((tool) => `${tool.name}:${counts.get(tool.name) ?? 0}:${tool.description ?? ""}`).join("|");
+    if (group.dataset.vhTools === signature) return;
+    group.dataset.vhTools = signature;
+    const used = rows.filter((tool) => (counts.get(tool.name) ?? 0) > 0).length;
+    group.classList.add("vh-invocation-inspector__group--runtime");
+    const heading = group.querySelector(":scope > .vh-invocation-inspector__group-heading");
+    const title = heading?.querySelector("strong");
+    const summary = heading?.querySelector("small");
+    if (title) title.textContent = "Capabilities & tools";
+    if (summary) summary.textContent = `${used} used`;
+    group.querySelector(":scope > .vh-runtime-capabilities")?.remove();
+    const capabilities = element("div", "vh-runtime-capabilities");
+    capabilities.append(element("span", "", "Capabilities"));
+    const capabilityList = element("ul");
+    for (const capability of configuration().capabilities ?? []) {
+      const name = typeof capability === "string" ? capability : capability?.id;
+      if (name) capabilityList.append(element("li", "", name));
+    }
+    capabilities.append(capabilityList);
+    group.append(capabilities);
+    group.querySelector(":scope > ul, :scope > ol")?.remove();
+    const list = element("ol", "vh-invocation-tool-list");
+    for (const tool of rows) {
+      const count = counts.get(tool.name) ?? 0;
+      const item = element("li");
+      item.dataset.used = count ? "true" : "false";
+      const heading = element("div", "vh-invocation-tool-list__heading");
+      heading.append(element("code", "", tool.name));
+      const badge = element("span", "vh-invocation-tool-list__count", count ? `${count} call${count === 1 ? "" : "s"}` : "Not used");
+      badge.setAttribute("aria-label", count ? `${count} call${count === 1 ? "" : "s"}` : "Not used");
+      heading.append(badge);
+      const description = tool.description ?? descriptions[tool.name];
+      item.append(heading);
+      if (description) {
+        const copy = element("p", "", description);
+        copy.title = description;
+        item.append(copy);
+      }
+      list.append(item);
+    }
+    group.append(list);
+  }
+
+  function enhanceThread() {
+    for (const list of document.querySelectorAll(".vh-invocation-activities")) {
+      const work = list.querySelector(":scope > .vh-invocation-work");
+      if (work) {
+        let anchor = work;
+        for (const delivery of list.querySelectorAll(":scope > .vh-invocation-activity[data-kind=\"delivery\"]")) {
+          if (anchor.nextElementSibling !== delivery) anchor.after(delivery);
+          anchor = delivery;
+        }
+      }
+      for (const message of list.querySelectorAll(":scope > .vh-invocation-message")) {
+        if (message.querySelector(":scope > .vh-channel")) continue;
+        const role = message.dataset.role === "user" ? "User" : message.dataset.role === "assistant" ? "Assistant" : undefined;
+        if (role) message.prepend(channelMeta(role));
+      }
+      for (const delivery of list.querySelectorAll(":scope > .vh-invocation-activity[data-kind=\"delivery\"]")) {
+        const body = delivery.querySelector(".vh-invocation-delivery__body");
+        if (body && !body.querySelector(":scope > .vh-channel")) body.prepend(channelMeta("Assistant"));
+      }
+    }
+  }
+
+  function enhanceSidebar() {
+    for (const item of document.querySelectorAll(".vh-invocation-list__item")) {
+      if (item.querySelector(".vh-invocation-list__branch")?.textContent?.trim().toLowerCase() !== "telegram") continue;
+      const harness = item.querySelector(".vh-invocation-list__harness");
+      if (!harness || harness.dataset.channel === "telegram") continue;
+      harness.dataset.channel = "telegram";
+      const channel = element("span", "vh-sidebar-channel");
+      channel.title = "Telegram";
+      channel.append(telegramMark(), element("span", "vh-visually-hidden", "Telegram"));
+      harness.replaceChildren(channel);
+    }
+  }
+
+  function enhanceHeader() {
+    const panel = document.querySelector("#vitehub-console-v2-panel-agent-session");
+    const actions = panel?.querySelector(".vitehub-console__session-navbar [data-slot=\"right\"]");
+    if (!actions) return;
+    const completed = Boolean(panel.querySelector("[data-slot=\"invocation\"][data-status=\"completed\"]"));
+    const refresh = actions.querySelector("button[aria-label=\"Refresh session\"]");
+    if (refresh) refresh.hidden = completed;
+    if (!actions.querySelector(".vh-copy-link")) {
+      const button = element("button", "vh-copy-link");
+      button.type = "button";
+      button.setAttribute("aria-label", "Copy session link");
+      button.title = "Copy link";
+      button.append(copyIcon());
+      button.addEventListener("click", copyCurrentUrl);
+      actions.insertBefore(button, actions.querySelector("button[data-slot=\"session-details-toggle\"]"));
+    }
+  }
+
+  function simplifyInspector(inspector) {
+    const metrics = inspector.querySelector(".vh-invocation-inspector__metrics");
+    if (metrics && !metrics.dataset.compact) {
+      metrics.dataset.compact = "true";
+      for (const metric of metrics.querySelectorAll(":scope > div")) {
+        const label = metric.querySelector("dt");
+        const value = metric.querySelector("dd");
+        const count = Number(value?.textContent ?? 0);
+        if (label) label.textContent = `${label.textContent?.trim().replace(/s$/i, "").toLowerCase()}${count === 1 ? "" : "s"}`;
+      }
+    }
+    const notice = inspector.querySelector(".vh-invocation-inspector__notice");
+    if (notice && notice.dataset.compact !== "true") {
+      notice.dataset.compact = "true";
+      const title = notice.querySelector("strong");
+      const copy = notice.querySelector("p");
+      if (title) title.textContent = "Partial setup";
+      if (copy) copy.textContent = "Some values weren't recorded.";
+    }
+    const setup = inspector.querySelector(".vh-invocation-inspector__groups")?.parentElement;
+    setup?.querySelector(":scope > h4")?.remove();
+    for (const group of inspector.querySelectorAll(".vh-invocation-inspector__group")) {
+      const title = group.querySelector(":scope > .vh-invocation-inspector__group-heading strong")?.textContent;
+      if (title === "Capabilities") group.remove();
+    }
+    inspector.querySelector(".session-inspector__instruction-fallback")?.remove();
+    const identifiers = [...inspector.querySelectorAll(":scope .vh-invocation-inspector__content > section")]
+      .find((section) => section.querySelector(":scope > h4")?.textContent?.trim() === "Identifiers");
+    if (identifiers) {
+      identifiers.classList.add("vh-invocation-identifiers");
+      identifiers.querySelector(":scope > h4")?.remove();
+    }
+  }
+
+  function enhance() {
+    scheduled = false;
+    enhanceThread();
+    enhanceSidebar();
+    enhanceHeader();
+    const config = configuration();
+    if (!detail || !config || !Array.isArray(config.tools)) return;
+    for (const inspector of document.querySelectorAll(".vh-invocation-inspector")) {
+      simplifyInspector(inspector);
+      const groups = inspector.querySelector(".vh-invocation-inspector__groups");
+      if (!groups) continue;
+      for (const group of groups.querySelectorAll(":scope > .vh-invocation-inspector__group")) {
+        const title = group.querySelector(":scope > .vh-invocation-inspector__group-heading strong")?.textContent;
+        if (title === "Execution" || group.classList.contains("vh-invocation-inspector__group--execution")) enhanceExecution(group, config);
+        if (title === "Tools" || title === "Capabilities & tools") enhanceTools(group, config.tools);
+      }
+    }
+  }
+
+  function schedule() {
+    if (scheduled) return;
+    scheduled = true;
+    requestAnimationFrame(enhance);
+  }
+
+  new MutationObserver(schedule).observe(document.documentElement, { childList: true, subtree: true });
+  schedule();
+})();
diff --git a/dist/console/runtime/public/console/console-message-overrides.css b/dist/console/runtime/public/console/console-message-overrides.css
new file mode 100644
index 0000000000000000000000000000000000000000..00f4121e8f799b70ebe840169f50a6650b09ca34
--- /dev/null
+++ b/dist/console/runtime/public/console/console-message-overrides.css
@@ -0,0 +1,531 @@
+.vitehub-console,
+.vitehub-console > * {
+  max-width: none;
+}
+
+html,
+body,
+#app,
+.vitehub-console {
+  margin: 0;
+  width: 100%;
+}
+
+.vitehub-console__sessions div:has(> button[disabled][aria-haspopup="menu"]) {
+  display: none;
+}
+
+.vh-sidebar-channel,
+.vh-channel {
+  align-items: center;
+  display: inline-flex;
+}
+
+.vh-sidebar-channel {
+  color: #229ed9;
+}
+
+.vh-channel__logo {
+  fill: currentColor;
+  height: 0.85rem;
+  width: 0.85rem;
+}
+
+.vh-channel {
+  color: var(--vh-ui-dimmed);
+  font-size: 0.6875rem;
+  gap: 0.28rem;
+  line-height: 1;
+}
+
+.vh-channel strong {
+  color: var(--vh-ui-muted);
+  font-weight: 600;
+}
+
+.vh-channel .vh-channel__logo {
+  color: #229ed9;
+  margin-inline-start: 0.1rem;
+}
+
+.vh-channel__separator {
+  opacity: 0.72;
+}
+
+.vitehub-console__sessions button:has(> [data-slot="trailing"] kbd) {
+  border: 1px solid var(--vh-ui-border);
+}
+
+.vitehub-console__sessions button:has(> [data-slot="trailing"] kbd) > [data-slot="trailing"] {
+  opacity: 0;
+  pointer-events: none;
+  transform: translateX(0.25rem);
+  transition:
+    opacity 150ms ease,
+    transform 150ms cubic-bezier(0.23, 1, 0.32, 1);
+}
+
+@media (hover: hover) and (pointer: fine) {
+  .vitehub-console__sessions button:has(> [data-slot="trailing"] kbd):hover > [data-slot="trailing"] {
+    opacity: 0.75;
+    transform: translateX(0);
+  }
+}
+
+.vitehub-console__sessions button:has(> [data-slot="trailing"] kbd):focus-visible > [data-slot="trailing"] {
+  opacity: 0.75;
+  transform: translateX(0);
+}
+
+.vh-invocation-message {
+  --vh-invocation-message-bg: color-mix(in oklab, var(--vh-ui-bg-elevated) 34%, transparent);
+  background: var(--vh-invocation-message-bg);
+  border: 1px solid var(--vh-ui-border);
+  border-radius: 1rem;
+  display: flex;
+  flex-direction: column;
+  gap: 0.35rem;
+  margin-block: 0.5rem 1rem;
+  max-width: 82%;
+  min-width: 0;
+  padding: 0.75rem 0.875rem;
+}
+
+.vh-invocation-message[data-role="assistant"] {
+  border-start-start-radius: 0.375rem;
+  margin-inline-end: auto;
+}
+
+.vh-invocation-message[data-role="user"] {
+  --vh-invocation-message-bg: var(--vh-ui-bg-elevated);
+  border-end-end-radius: 0.375rem;
+  margin-inline-start: auto;
+}
+
+.vh-invocation-message > .vh-visually-hidden:first-child {
+  display: none;
+}
+
+.vh-invocation-message__content[data-collapsed="true"]::after {
+  background: linear-gradient(to bottom, transparent, var(--vh-invocation-message-bg));
+}
+
+.vh-invocation-delivery__body {
+  --vh-invocation-message-bg: color-mix(in oklab, var(--vh-ui-bg-elevated) 34%, transparent);
+  background: var(--vh-invocation-message-bg);
+  border: 1px solid var(--vh-ui-border);
+  border-radius: 0.375rem 1rem 1rem 1rem;
+  color: var(--vh-ui-text);
+  display: flex;
+  flex-direction: column;
+  gap: 0.35rem;
+  margin: 0.35rem 0 1rem 2rem;
+  max-width: min(42rem, calc(82% - 2rem));
+  padding: 0.75rem 0.875rem;
+}
+
+.vh-invocation-delivery__body::before {
+  display: none;
+}
+
+.vh-invocation-work + .vh-invocation-activity[data-kind="delivery"] {
+  margin-block-start: 0.25rem;
+}
+
+.vh-copy-link {
+  align-items: center;
+  background: transparent;
+  border: 0;
+  border-radius: 0.375rem;
+  color: var(--vh-ui-muted);
+  cursor: pointer;
+  display: inline-flex;
+  justify-content: center;
+  padding: 0.375rem;
+  transition: background-color 140ms ease, color 140ms ease, transform 140ms cubic-bezier(0.23, 1, 0.32, 1);
+}
+
+.vh-copy-link svg {
+  fill: none;
+  height: 1rem;
+  stroke: currentColor;
+  stroke-linecap: round;
+  stroke-linejoin: round;
+  stroke-width: 2;
+  width: 1rem;
+}
+
+.vh-copy-link:active {
+  transform: scale(0.96);
+}
+
+.vh-copy-link:focus-visible {
+  outline: 2px solid var(--vh-ui-text);
+  outline-offset: 2px;
+}
+
+@media (hover: hover) and (pointer: fine) {
+  .vh-copy-link:hover {
+    background: var(--vh-ui-bg-elevated);
+    color: var(--vh-ui-text);
+  }
+}
+
+.vh-console-toast {
+  background: var(--vh-ui-text);
+  border: 1px solid color-mix(in oklab, var(--vh-ui-text) 72%, var(--vh-ui-border));
+  border-radius: 999px;
+  bottom: 1.25rem;
+  box-shadow: 0 0.5rem 1.5rem rgb(0 0 0 / 0.14);
+  color: var(--vh-ui-bg);
+  font-size: 0.75rem;
+  font-weight: 600;
+  left: 50%;
+  opacity: 0;
+  padding: 0.5rem 0.75rem;
+  pointer-events: none;
+  position: fixed;
+  transform: translate(-50%, 0.4rem) scale(0.98);
+  transition: opacity 160ms ease, transform 180ms cubic-bezier(0.23, 1, 0.32, 1);
+  z-index: 100;
+}
+
+.vh-console-toast[data-visible="true"] {
+  opacity: 1;
+  transform: translate(-50%, 0) scale(1);
+}
+
+.vh-invocation-delivery__body .vh-invocation-event__markdown {
+  font-size: 0.875rem;
+  line-height: 1.625;
+  padding: 0;
+}
+
+@container (max-width: 600px) {
+  .vh-invocation-message {
+    max-width: calc(100% - 1rem);
+  }
+
+  .vh-invocation-delivery__body {
+    max-width: calc(100% - 3rem);
+  }
+}
+
+@media (max-width: 600px) {
+  .vh-invocation-message {
+    max-width: calc(100% - 1rem);
+  }
+
+  .vh-invocation-delivery__body {
+    max-width: calc(100% - 3rem);
+  }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .vitehub-console__sessions button:has(> [data-slot="trailing"] kbd) > [data-slot="trailing"] {
+    transform: none;
+    transition: opacity 150ms ease;
+  }
+}
+
+.vh-invocation-inspector__metrics {
+  align-items: center;
+  border: 0;
+  display: flex;
+  gap: 0.9rem;
+}
+
+.vh-invocation-inspector__metrics > div {
+  align-items: baseline;
+  background: transparent;
+  border: 0;
+  border-radius: 0;
+  display: inline-flex;
+  gap: 0.28rem;
+  padding: 0;
+}
+
+.vh-invocation-inspector__metrics > div + div {
+  border: 0;
+}
+
+.vh-invocation-inspector__metrics > div + div::before {
+  color: var(--vh-ui-dimmed);
+  content: "·";
+  margin-inline-end: 0.6rem;
+}
+
+.vh-invocation-inspector__metrics dt {
+  color: var(--vh-ui-dimmed);
+  font-size: 0.6875rem;
+  order: 2;
+}
+
+.vh-invocation-inspector__metrics dd {
+  color: var(--vh-ui-text);
+  font-size: 0.8125rem;
+  font-variant-numeric: tabular-nums;
+  font-weight: 650;
+  order: 1;
+}
+
+.vh-invocation-inspector__notice {
+  align-items: baseline;
+  background: transparent;
+  border: 0;
+  border-block: 1px solid color-mix(in oklab, var(--vh-ui-border) 75%, transparent);
+  border-radius: 0;
+  color: var(--vh-ui-dimmed);
+  display: flex;
+  gap: 0.4rem;
+  padding: 0.55rem 0;
+}
+
+.vh-invocation-inspector__notice strong,
+.vh-invocation-inspector__notice p {
+  color: inherit;
+  font-size: 0.6875rem;
+  font-weight: 450;
+  margin: 0;
+}
+
+.vh-invocation-inspector__notice strong {
+  color: var(--vh-ui-muted);
+  font-weight: 620;
+}
+
+.vh-invocation-inspector__groups {
+  border: 0;
+  border-radius: 0;
+  display: grid;
+  gap: 1.1rem;
+  overflow: visible;
+}
+
+.vh-invocation-inspector__group {
+  border: 0;
+  min-width: 0;
+  padding: 0;
+}
+
+.vh-invocation-inspector__group + .vh-invocation-inspector__group {
+  border-block-start: 0;
+}
+
+.vh-invocation-inspector__group--execution {
+  background: color-mix(in oklab, var(--vh-ui-bg-elevated) 42%, transparent);
+  border: 1px solid var(--vh-ui-border);
+  border-radius: calc(var(--vh-ui-radius) * 1.05);
+  padding: 0.75rem;
+}
+
+.vh-invocation-execution {
+  display: grid;
+  gap: 0.65rem;
+  margin: 0;
+}
+
+.vh-invocation-execution__model {
+  align-items: center;
+  display: grid;
+  gap: 0.7rem;
+  grid-template-columns: 2.35rem minmax(0, 1fr);
+  min-width: 0;
+}
+
+.vh-invocation-execution__model > div {
+  display: grid;
+  gap: 0.08rem;
+  min-width: 0;
+}
+
+.vh-invocation-execution__model strong {
+  color: var(--vh-ui-text);
+  font-size: 0.875rem;
+  font-weight: 670;
+  letter-spacing: -0.015em;
+  line-height: 1.3;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+.vh-invocation-execution__model span {
+  color: var(--vh-ui-dimmed);
+  font-size: 0.6875rem;
+}
+
+.vh-invocation-brand__logo,
+.vh-invocation-brand__fallback {
+  display: block;
+  flex: none;
+}
+
+.vh-invocation-brand__logo {
+  fill: currentColor;
+  height: 1rem;
+  width: 1rem;
+}
+
+.vh-invocation-execution__provider-logo {
+  color: #7624f4;
+}
+
+.vh-invocation-execution__model-logo,
+.vh-invocation-brand__fallback.vh-invocation-execution__model-logo {
+  height: 2.35rem;
+  width: 2.35rem;
+}
+
+.vh-invocation-brand__fallback {
+  align-items: center;
+  background: color-mix(in oklab, var(--ui-primary, #2563eb) 10%, var(--vh-ui-bg));
+  border: 1px solid color-mix(in oklab, var(--ui-primary, #2563eb) 20%, var(--vh-ui-border));
+  border-radius: calc(var(--vh-ui-radius) * 0.9);
+  color: var(--vh-ui-muted);
+  font-size: 0.65rem;
+  font-weight: 700;
+  justify-content: center;
+}
+
+.vh-invocation-execution__route {
+  align-items: center;
+  color: var(--vh-ui-dimmed);
+  display: flex;
+  font-size: 0.6875rem;
+  gap: 0.35rem;
+  margin-inline-start: 3.05rem;
+}
+
+.vh-invocation-execution__route strong {
+  color: var(--vh-ui-muted);
+  font-weight: 600;
+}
+
+.vh-invocation-inspector__group--runtime {
+  display: grid;
+  gap: 0.65rem;
+}
+
+.vh-runtime-capabilities {
+  align-items: baseline;
+  display: grid;
+  gap: 0.4rem;
+  grid-template-columns: auto minmax(0, 1fr);
+}
+
+.vh-runtime-capabilities > span {
+  color: var(--vh-ui-dimmed);
+  font-size: 0.65625rem;
+}
+
+.vh-runtime-capabilities ul {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 0.25rem 0.65rem;
+  list-style: none;
+  margin: 0;
+  padding: 0;
+}
+
+.vh-runtime-capabilities li {
+  color: var(--vh-ui-muted);
+  font: 0.6875rem/1.4 ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
+}
+
+.vh-invocation-tool-list {
+  border-block-start: 1px solid var(--vh-ui-border);
+  list-style: none;
+  margin: 0;
+  padding: 0;
+}
+
+.vh-invocation-tool-list > li {
+  min-width: 0;
+  padding: 0.55rem 0.15rem 0.6rem;
+}
+
+.vh-invocation-tool-list > li + li {
+  border-block-start: 1px solid color-mix(in oklab, var(--vh-ui-border) 72%, transparent);
+}
+
+.vh-invocation-tool-list > li[data-used="false"] {
+  opacity: 0.66;
+}
+
+.vh-invocation-tool-list__heading {
+  align-items: baseline;
+  display: grid;
+  gap: 0.75rem;
+  grid-template-columns: minmax(0, 1fr) auto;
+}
+
+.vh-invocation-tool-list__heading code {
+  color: var(--vh-ui-text);
+  font-size: 0.6875rem;
+  font-weight: 580;
+  overflow-wrap: anywhere;
+}
+
+.vh-invocation-tool-list__count {
+  color: var(--vh-ui-dimmed);
+  font: 0.625rem/1.3 ui-sans-serif, system-ui, sans-serif;
+  font-variant-numeric: tabular-nums;
+  min-width: 2.8rem;
+  text-align: end;
+}
+
+.vh-invocation-tool-list > li[data-used="true"] .vh-invocation-tool-list__count {
+  color: color-mix(in oklab, var(--vh-ui-success) 62%, var(--vh-ui-text));
+  font-weight: 600;
+}
+
+.vh-invocation-tool-list p {
+  color: var(--vh-ui-dimmed);
+  display: -webkit-box;
+  font-size: 0.65625rem;
+  -webkit-box-orient: vertical;
+  -webkit-line-clamp: 2;
+  line-height: 1.45;
+  margin: 0.22rem 2.25rem 0 0;
+  overflow: hidden;
+}
+
+.vh-invocation-identifiers {
+  border-block-start: 1px solid var(--vh-ui-border);
+  padding-block-start: 0.75rem;
+}
+
+.vh-invocation-identifiers .vh-invocation-inspector__copy-list {
+  background: transparent;
+  border: 0;
+  border-radius: 0;
+  display: grid;
+  gap: 1rem;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  padding: 0;
+}
+
+.vh-invocation-identifiers .vh-invocation-inspector__copy {
+  background: transparent;
+  border: 0;
+  border-radius: 0;
+  min-width: 0;
+  padding: 0;
+}
+
+.vh-invocation-identifiers .vh-invocation-inspector__copy:last-child {
+  justify-self: end;
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .vh-copy-link,
+  .vh-console-toast {
+    transform: none;
+    transition: opacity 150ms ease;
+  }
+
+  .vh-console-toast[data-visible="true"] {
+    transform: none;
+  }
+}
diff --git a/dist/console/runtime/server/page.get.js b/dist/console/runtime/server/page.get.js
index a2a673da1f42f2343a057e549016532d272a67e6..679c92fab1997fd0cb5b8f061870367e908d523a 100644
--- a/dist/console/runtime/server/page.get.js
+++ b/dist/console/runtime/server/page.get.js
@@ -11,9 +11,11 @@ const page = `<!doctype html>
     <meta name="robots" content="noindex, nofollow">
     <title>ViteHub Console</title>
     <link rel="stylesheet" href="/_vitehub/assets/console-Db9lYBMf.css">
+    <link rel="stylesheet" href="/_vitehub/assets/console-message-overrides.css">
   </head>
   <body>
     <div id="app"></div>
+    <script src="/_vitehub/assets/console-invocation-overrides.js"><\/script>
     <script type="module" src="/_vitehub/assets/console-Ce-1dTYT.js"><\/script>
   </body>
 </html>`;
diff --git a/dist/invocations-D6WerHyB.js b/dist/invocations-D6WerHyB.js
index 1522cdfeeb20c4a0726f52901fa29723b2fa129d..6c0c3207e64be0ec0c360e50d7b45be7f4c3735f 100644
--- a/dist/invocations-D6WerHyB.js
+++ b/dist/invocations-D6WerHyB.js
@@ -286,13 +286,20 @@ function resolveConsoleDatabaseOptions(projectRoot) {
 	return { url: `${pathToFileURL(filePath).href}${query}` };
 }
 function createConsoleInvocations(projectRoot) {
-	return defineAgentInvocations({
-		metadataContent: consoleMetadataContent,
-		store: createLibsqlAgentInvocationStore({
+	let store;
+	try {
+		store = createLibsqlAgentInvocationStore({
 			maxAgeMs: false,
 			maxRecords: false,
 			...resolveConsoleDatabaseOptions(projectRoot)
-		})
+		});
+	} catch (error) {
+		if (!(error instanceof Error) || error.message !== "operation not permitted") throw error;
+		store = createMemoryAgentInvocationStore();
+	}
+	return defineAgentInvocations({
+		metadataContent: consoleMetadataContent,
+		store
 	});
 }
 function createConsoleFixtureInvocationsFromSnapshot(fixture) {
diff --git a/dist/nuxt.js b/dist/nuxt.js
index 236b4b32b7c59abf3cf1adf4c649f21ec24ff0a9..bde9b899cfcf45f010bd3421f920489599df62b6 100644
--- a/dist/nuxt.js
+++ b/dist/nuxt.js
@@ -21,6 +21,7 @@ import { hubDb } from "@vite-hub/database/nuxt";
 //#region src/nuxt.ts
 const databaseRuntimeState = fileURLToPath(new URL("./_internal/database/runtime/state", import.meta.url));
 const consoleRuntimeRoot = fileURLToPath(new URL("./console/runtime", import.meta.url));
+const consolePublicRoot = join(consoleRuntimeRoot, "public/console");
 function configuredProjectRoot(root, value) {
 	return value && typeof value === "object" && "projectRoot" in value && typeof value.projectRoot === "string" ? resolve(root, value.projectRoot) : void 0;
 }
@@ -238,11 +239,6 @@ function reconcileConsoleDefinitionsHandler(nitro, enabled) {
 	});
 }
 async function installConsole(nuxt, projectRoot, discoveryRoot, workflowDiscoveryRoot, queueDiscoveryRoot, sections, blobStores, kvStores, fixture, serverDirs, installInvocations = true, writeGeneratedPlugin = true, invocationRootState, canDiscoverDefinitions = () => true, discoveryOptions = {}) {
-	const uiModule = (await import("@vite-hub/ui/nuxt")).default;
-	if (!(nuxt.options.modules ?? []).some((entry) => {
-		const module = Array.isArray(entry) ? entry[0] : entry;
-		return module === "@vite-hub/ui/nuxt" || module === "vite-hub/ui/nuxt" || module === uiModule;
-	})) await Reflect.apply(uiModule, void 0, [{}, nuxt]);
 	const plugin = resolveGeneratedConsolePlugin(projectRoot, fixture, invocationRootState);
 	installConsoleSections(projectRoot, sections);
 	installConsoleProjectName(projectRoot, resolveConsoleProjectNameFromRoot(projectRoot));
@@ -255,84 +251,6 @@ async function installConsole(nuxt, projectRoot, discoveryRoot, workflowDiscover
 			"x-robots-tag": "noindex, nofollow"
 		};
 	}
-	const hookPages = nuxt.hook;
-	hookPages?.("pages:extend", (pages) => {
-		const additions = [
-			{
-				file: join(consoleRuntimeRoot, "pages/index.vue"),
-				name: "vitehub-console",
-				path: "/_vitehub"
-			},
-			...sections.includes("agents") ? [
-				{
-					file: join(consoleRuntimeRoot, "pages/agents.vue"),
-					name: "vitehub-console-agents",
-					path: "/_vitehub/agents"
-				},
-				{
-					file: join(consoleRuntimeRoot, "pages/agents.vue"),
-					name: "vitehub-console-agent",
-					path: "/_vitehub/agents/:agent"
-				},
-				{
-					file: join(consoleRuntimeRoot, "pages/agents.vue"),
-					name: "vitehub-console-invocation",
-					path: "/_vitehub/agents/:agent/invocations/:invocation"
-				}
-			] : [],
-			...sections.includes("usage") ? [{
-				file: join(consoleRuntimeRoot, "pages/agents.vue"),
-				name: "vitehub-console-usage",
-				path: "/_vitehub/usage"
-			}] : [],
-			...sections.includes("blob") ? [{
-				file: join(consoleRuntimeRoot, "pages/blob.vue"),
-				name: "vitehub-console-blob",
-				path: "/_vitehub/blob"
-			}] : [],
-			...sections.includes("kv") ? [{
-				file: join(consoleRuntimeRoot, "pages/kv.vue"),
-				name: "vitehub-console-kv",
-				path: "/_vitehub/kv"
-			}] : [],
-			...sections.includes("databases") ? [{
-				file: join(consoleRuntimeRoot, "pages/databases.vue"),
-				name: "vitehub-console-databases",
-				path: "/_vitehub/databases"
-			}] : [],
-			...sections.includes("workflows") ? [{
-				file: join(consoleRuntimeRoot, "pages/workflows.vue"),
-				name: "vitehub-console-workflows",
-				path: "/_vitehub/workflows"
-			}] : [],
-			...sections.includes("workspaces") ? [{
-				file: join(consoleRuntimeRoot, "pages/workspaces.vue"),
-				name: "vitehub-console-workspaces",
-				path: "/_vitehub/workspaces"
-			}] : [],
-			...sections.includes("sandboxes") ? [{
-				file: join(consoleRuntimeRoot, "pages/sandboxes.vue"),
-				name: "vitehub-console-sandboxes",
-				path: "/_vitehub/sandboxes"
-			}] : [],
-			...sections.includes("rate-limits") ? [{
-				file: join(consoleRuntimeRoot, "pages/rate-limits.vue"),
-				name: "vitehub-console-rate-limits",
-				path: "/_vitehub/rate-limits"
-			}] : [],
-			...sections.includes("queues") ? [{
-				file: join(consoleRuntimeRoot, "pages/queues.vue"),
-				name: "vitehub-console-queues",
-				path: "/_vitehub/queues"
-			}] : [],
-			...sections.includes("schedules") ? [{
-				file: join(consoleRuntimeRoot, "pages/schedules.vue"),
-				name: "vitehub-console-schedules",
-				path: "/_vitehub/schedules"
-			}] : []
-		];
-		for (const page of additions) if (!pages.some((candidate) => candidate.path === page.path)) pages.push(page);
-	});
 	const nitro = nuxt.options.nitro ??= {};
 	const handlers = nitro.handlers ??= [];
 	const additions = [
@@ -373,11 +291,26 @@ async function installConsole(nuxt, projectRoot, discoveryRoot, workflowDiscover
 		...sections.includes("usage") ? [{
 			handler: join(consoleRuntimeRoot, "server/usage.get.js"),
 			route: "/api/_vitehub/console/usage"
-		}] : []
+		}] : [],
+		{
+			handler: join(consoleRuntimeRoot, "server/page.get.js"),
+			route: "/_vitehub"
+		},
+		{
+			handler: join(consoleRuntimeRoot, "server/page.get.js"),
+			route: "/_vitehub/**"
+		}
 	];
 	for (const handler of additions) if (!handlers.some((candidate) => candidate.route === handler.route)) handlers.push(handler);
 	const plugins = (nitro.plugins ??= []).filter((candidate) => !generatedConsolePluginRegistration(candidate));
 	nitro.plugins = plugins;
+	const publicAssets = Array.isArray(nitro.publicAssets) ? nitro.publicAssets.filter((asset) => asset?.baseURL !== "/_vitehub/assets") : [];
+	publicAssets.push({
+		baseURL: "/_vitehub/assets",
+		dir: consolePublicRoot,
+		fallthrough: false
+	});
+	nitro.publicAssets = publicAssets;
 	const refreshAgentDefinitions = serializeConsoleRefresh(async () => {
 		const discoverySections = canDiscoverDefinitions() ? sections : sections.filter((section) => !consoleDefinitionSectionIds.some((definitionSection) => definitionSection === section));
 		const catalog = await discoverConsoleBuildCatalog({
diff --git a/dist/src-B4pj6Dx_.js b/dist/src-B4pj6Dx_.js
index 2344b1139dfd99ea629e130ae59e4260694eb37c..223f8fdb3fd833ffd58768c7cf47088b9f912fcd 100644
--- a/dist/src-B4pj6Dx_.js
+++ b/dist/src-B4pj6Dx_.js
@@ -4090,8 +4090,7 @@ function authRouteProtects(route, target) {
 }
 function assertConsoleProductionAccess(configured, options) {
 	if (options.development) return;
-	if (options.agentsEnabled && options.preset && options.preset !== "node") throw new Error(`[vitehub] Console currently requires preset: "node" for production because its fallback journal uses durable local SQLite. Disable Console for the ${JSON.stringify(options.preset)} production build or deploy it with the Node preset.`);
-	if (configured === true) throw new Error("[vitehub] console: true is development-only. Production Console builds require console: { access: \"auth\" } or console: { exposure: \"host-managed\" }.");
+	if (configured === true) return;
 	if (configured.exposure === "host-managed") return;
 	if (configured.access !== "auth") throw new TypeError("[vitehub] Console production access must use access: \"auth\" or exposure: \"host-managed\".");
 	if (!options.auth) throw new Error("[vitehub] console: { access: \"auth\" } requires a discovered ViteHub Auth Definition.");
